Hay q tener python3, docker, docker-compose


2 formas de generar consultas (uniform y zipf) ---------------------------------------  en main.py

3 Espacios para memoria cache (50MB, 100MB, 200MB) -> (2MB, 4MB, 8MB) ----------------  en docker-compose.yml

2 Politicas de remocion LRU y LFU --------- allkeys-lru ------ allkeys-lfu -----------  en docker-compose.yml


cambio de TTL para ver como afecta (en uniform, 2MB, LRU) ----------------------------- en engine.py


Video

Obj general
1) analizar el comportamiento de un sistema cache implementado en Redis. viendo como afectan cosas como la distribucion de las consultas, 
la politica de remocion (LRU, LFU), el tamaño asignado a memoria cache y TTL

2) Este sistema se implemento utilizando dockers y python para los scripts, utilizando las siguientes librerias: pandas para extraer los datos,
numpy para trabajar los datos y redis para hacer el sistema cache

3) data_load.py para cargar el dataset
4) queries.py para hacer las peticiones
5) traffic_gen.py para generar trafico sintetico a partir de las queries y siguiendo las distribuciones
zipf o uniforme
6)

requirements.txt

data_load.py -> queries.py

